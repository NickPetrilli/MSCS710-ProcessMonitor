// import React, { useState } from 'react';
// import './TextBox.css'; // Import CSS file for styling

// const UtilTextBox = () => {

//   // Define a function to determine the background color based on the text content
//   const getUtilBackgroundColor = (text) => {
//     const percentage = parseInt(text.substring(0, text.indexOf("U")).trim());
//     if (percentage < 40) {
//       return 'green';
//     } else if (percentage >= 40 && percentage < 80) {
//       return 'orange';
//     } else {
//       return 'red';
//     }
//   };

//   return (
//     <div className="utilBox" style={{ backgroundColor }}> {text} </div>
//   );
// };

// export default UtilTextBox;